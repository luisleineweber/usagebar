(function () {
  globalThis.__openusage_plugin = {
    id: "openrouter",
    probe: function () {
      throw "OpenRouter is a Windows placeholder. Implementation is planned but not available yet."
    },
  }
})()
