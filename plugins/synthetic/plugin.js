(function () {
  globalThis.__openusage_plugin = {
    id: "synthetic",
    probe: function () {
      throw "Synthetic is a Windows placeholder. Implementation is planned but not available yet."
    },
  }
})()
