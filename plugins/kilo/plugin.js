(function () {
  globalThis.__openusage_plugin = {
    id: "kilo",
    probe: function () {
      throw "Kilo is a Windows placeholder. Implementation is planned but not available yet."
    },
  }
})()
