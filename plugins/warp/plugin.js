(function () {
  globalThis.__openusage_plugin = {
    id: "warp",
    probe: function () {
      throw "Warp is a Windows placeholder. Implementation is planned but not available yet."
    },
  }
})()
