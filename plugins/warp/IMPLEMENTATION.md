# Warp Windows Placeholder

Status: visible in Settings, blocked from probing on Windows.

Planned implementation:
- Save a Warp account token in the app-owned secret store.
- Query the account limits endpoint directly.
- Keep the first Windows slice token based, not browser based.
