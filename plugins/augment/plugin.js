(function () {
  globalThis.__openusage_plugin = {
    id: "augment",
    probe: function () {
      throw "Augment is a Windows placeholder. Implementation is planned but not available yet."
    },
  }
})()
