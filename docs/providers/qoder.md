# Qoder

Qoder reads Big Model credits from `qoder.com` or `qoder.com.cn`.

Save a Cookie request header and the matching host in Settings. Origin and Referer use the same host.

UsageBar combines `totalQuota` and `sharedQuota`. It keeps provider-reported zero separate from missing data.

Automatic browser cookie import is not part of this Windows path.
