# Qwen Code

UsageBar reads Qwen Code's local history through `ccusage qwen daily`. The graph shows local token activity and estimated API-equivalent cost; it is not a Qwen subscription quota.

Use Qwen Code locally first, then refresh UsageBar. History stays on this device.
