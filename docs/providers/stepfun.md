# StepFun

StepFun reads five-hour and weekly Step Plan quota.

Save an Oasis-Token in Settings. You can also set `STEPFUN_TOKEN`.

UsageBar requests rate-limit data first. Plan status is optional. A plan-status error does not remove valid quota data.

The username and password login flow is not part of this manual Windows path.
