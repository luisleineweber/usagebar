# Devin

Devin reads daily and weekly quota for one organization.

Save a bearer token and an internal `org_...` organization ID in Settings. UsageBar does not guess the organization.

You can also set `DEVIN_BEARER_TOKEN` and `DEVIN_ORGANIZATION`.

UsageBar sends the token only to `app.devin.ai`. It does not read Chrome Local Storage in this Windows path.
